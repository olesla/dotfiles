# Fonts

## Installation
Create the folder `~/.fonts` before running stow. Once that is done, proceed by rebuilding the cached list of fonts to include the newly installed ones.

```bash
fc-cache -fv
```

